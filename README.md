# indexeddb-handler
a small js class to handle using the indexedDB in a straightforward way

## TODO
- [x] add persistence check and try to persist storage if not configured otherwise (default true)
- [x] add strict durability
- [ ] more bug testing
- [ ] more documentation